{
  "name": "@diazstg/memory-bank-mcp",
  "version": "1.8.0",
  "description": "MCP Server for managing Memory Bank with remote server support",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "memory-bank-mcp": "build/index.js"
  },
  "scripts": {
    "clean": "rm -rf build",
    "build": "bun run clean && bun build src/index.ts --outdir build --target node",
    "start": "bun build/index.js",
    "dev": "bun --watch src/index.ts",
    "build:start": "bun run build && bun run start",
    "build:ext": "cd vscode-extension && npm run compile && npm run package",
    "release:ext": "bash -c 'set -e; BRANCH=$(git rev-parse --abbrev-ref HEAD); if [ \"$BRANCH\" != \"main\" ]; then echo \"ERROR: release:ext must be run from main (current: $BRANCH)\"; exit 1; fi; cd vscode-extension; VER=$(node -p \"require(\\\"./package.json\\\").version\"); echo \"Building vscode-extension v$VER...\"; npm run compile; npx @vscode/vsce package --no-dependencies; echo \"Tagging vscode-v$VER...\"; cd ..; git tag \"vscode-v$VER\"; git push origin \"vscode-v$VER\"; echo \"Done — workflow will create GitHub Release with .vsix\"'",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "prepublishOnly": "bun run build",
    "release": "standard-version",
    "release:minor": "standard-version --release-as minor",
    "release:major": "standard-version --release-as major",
    "release:patch": "standard-version --release-as patch",
    "release:push": "git push --follow-tags origin main"
  },
  "keywords": [
    "mcp",
    "memory-bank",
    "ai",
    "assistant",
    "context"
  ],
  "author": "Daniel Diaz Santiago (diaz3618)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/diaz3618/memory-bank-mcp.git"
  },
  "bugs": {
    "url": "https://github.com/diaz3618/memory-bank-mcp/issues"
  },
  "homepage": "https://github.com/diaz3618/memory-bank-mcp#readme",
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@types/js-yaml": "4.0.9",
    "@types/node": "^20.4.5",
    "fs-extra": "^11.1.1",
    "js-yaml": "4.1.1",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/bun": "^1.2.4",
    "@types/fs-extra": "^11.0.1",
    "standard-version": "^9.5.0",
    "ts-node": "10.9.2",
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "directories": {
    "doc": "docs"
  },
  "overrides": {
    "lodash": "4.17.23"
  }
}
